name: Build and Cache

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1" # weekly rebuild to keep in cache

jobs:
  build:
    name: ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: hikari
            target: darwinConfigurations.hikari.system
            os: macos-latest
          - name: ronri
            target: nixosConfigurations.ronri.config.system.build.toplevel
            os: ubuntu-latest
          - name: ito
            target: nixosConfigurations.ito.config.system.build.toplevel
            os: ubuntu-latest
          - name: kako
            target: homeConfigurations.elenah.activationPackage
            os: ubuntu-24.04-arm
          - name: devshell-x86_64-linux
            target: devShells.x86_64-linux.default
            os: ubuntu-latest
          - name: devshell-aarch64-linux
            target: devShells.aarch64-linux.default
            os: ubuntu-24.04-arm
          - name: devshell-aarch64-darwin
            target: devShells.aarch64-darwin.default
            os: macos-latest

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Free disk space
        if: runner.os == 'Linux'
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          swap-storage: true

      - name: Fix action runner security config
        if: runner.os == 'Linux'
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0

      - uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
            max-jobs = auto
            cores = 0

      - name: Install and configure Attic
        env:
          ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
          ATTIC_ENDPOINT: ${{ vars.ATTIC_ENDPOINT }}
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
        run: |
          nix profile install nixpkgs#attic-client
          attic login ci "$ATTIC_ENDPOINT" "$ATTIC_TOKEN"
          attic use "ci:$ATTIC_CACHE"

      - name: Build and push ${{ matrix.name }}
        env:
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
          EVENT: ${{ github.event_name }}
        run: |
          extra_args=()
          if [ "$EVENT" != "schedule" ]; then
            extra_args+=(--skip-cached)
          fi
          nix run nixpkgs#nix-fast-build -- \
            "${extra_args[@]}" \
            --no-nom \
            --flake ".#${{ matrix.target }}" \
            --attic-cache "ci:$ATTIC_CACHE"
